File now saves new card data to a dataframe and reports if a card has already been swiped

Future work:
    GPIO pin functions on PI 5
    cache users swiping out
    delete users swiping out from dataframe